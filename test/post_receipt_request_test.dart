import 'package:test/test.dart';
import 'package:matrix_dart_generated_client/openapi.dart';

// tests for PostReceiptRequest
void main() {
  final instance = PostReceiptRequestBuilder();
  // TODO add properties to the builder and call build()

  group(PostReceiptRequest, () {
    // The root thread event's ID (or `main`) for which thread this receipt is intended to be under. If not specified, the read receipt is *unthreaded* (default).
    // String threadId
    test('to test the property `threadId`', () async {
      // TODO
    });

  });
}
