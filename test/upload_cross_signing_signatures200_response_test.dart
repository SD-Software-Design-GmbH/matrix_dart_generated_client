import 'package:test/test.dart';
import 'package:matrix_dart_generated_client/openapi.dart';

// tests for UploadCrossSigningSignatures200Response
void main() {
  final instance = UploadCrossSigningSignatures200ResponseBuilder();
  // TODO add properties to the builder and call build()

  group(UploadCrossSigningSignatures200Response, () {
    // A map from user ID to key ID to an error for any signatures that failed.  If a signature was invalid, the `errcode` will be set to `M_INVALID_SIGNATURE`.
    // JsonObject failures
    test('to test the property `failures`', () async {
      // TODO
    });

  });
}
